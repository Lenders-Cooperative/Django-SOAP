============
Installation
============

At the command line::

    $ easy_install django-soap

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-soap
    $ pip install django-soap
